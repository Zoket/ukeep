name: Build and Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    outputs:
      image_tag: ${{ steps.meta.outputs.tags }}
      image_name: ${{ steps.image_names.outputs.image_name }}
      base_image_name: ${{ steps.image_names.outputs.base_image_name }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Normalize Image Names
        id: image_names
        shell: bash
        run: |
          echo "image_name=${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"
          echo "base_image_name=${GITHUB_REPOSITORY,,}-base" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for base image
        id: meta-base
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ steps.image_names.outputs.base_image_name }}
          tags: |
            type=raw,value=latest

      - name: Build and push base image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.base
          push: true
          tags: ${{ steps.meta-base.outputs.tags }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ steps.image_names.outputs.base_image_name }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ steps.image_names.outputs.base_image_name }}:buildcache,mode=max

      - name: Update Dockerfile to use registry base image
        run: |
          sed -i "s|FROM ukeep-base:latest|FROM ${{ env.REGISTRY }}/${{ steps.image_names.outputs.base_image_name }}:latest|g" docker/Dockerfile

      - name: Extract metadata for main image
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ steps.image_names.outputs.image_name }}
          tags: |
            type=raw,value=latest
            type=sha,prefix=

      - name: Build and push main image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ steps.image_names.outputs.image_name }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ steps.image_names.outputs.image_name }}:buildcache,mode=max

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            echo "Logging in to GitHub Container Registry..."
            echo ${{ secrets.GHCR_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            echo "Pulling latest image..."
            docker pull ${{ env.REGISTRY }}/${{ needs.build-and-push.outputs.image_name }}:latest

            echo "Stopping old container..."
            docker stop ukeep 2>/dev/null || true
            docker rm ukeep 2>/dev/null || true

            echo "Starting new container..."
            docker run -d \
              --name ukeep \
              -p 80:80 \
              --restart unless-stopped \
              ${{ env.REGISTRY }}/${{ needs.build-and-push.outputs.image_name }}:latest

            echo "Verifying deployment..."
            sleep 3
            docker ps | grep ukeep

            echo "Cleaning up old images..."
            docker image prune -f
