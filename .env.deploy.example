# VPS 部署配置
# 复制此文件为 .env.deploy 并填写实际值
# .env.deploy 已被 .gitignore 忽略，不会上传到 Git

VPS_USER="your-username"
VPS_HOST="your-vps-ip-or-domain"
VPS_PORT="22"
VPS_DEPLOY_PATH="/home/your-username/docker_compose/ukeep"

# SSH 私钥路径（可选，留空则使用默认 ~/.ssh/id_rsa 或 ~/.ssh/id_ed25519）
VPS_SSH_KEY_PATH=""
